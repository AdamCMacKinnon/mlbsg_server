/**
 * File contains a collection of the base URL and various endpoints of the
 * MLB Stats API.  Will update as it goes, however params will need to be filled out at various times
 * depending on use case.
 */

export const baseUrl = 'https://statsapi.mlb.com/api/v2';
export const currentDayEndpoint = 'schedule?sportId=1';
